Poner colores en printf -> update: para el cli me hice un par de macros para imprimir, aunq termine haciendo q salga por log (excepto interaccion con usuario) q me parece mejor q tener una sola consola impriiendo todos los procesos. No toque los prints de los otros procesos para no pisar los fuentes q estabas usando.



/** Christian: estas cosas creo q las estabas trabajando vos. Por las dudas no toque las clases de cine y admin. Si ya lo terminaste avisame y cuando termino con el cliente lo pruebo todo. Y si te falta algo te ayudo cuando termine con el cliente **/

alarma de tiempo de sesion

mover la alarma a cine hijo

en timeout, avisar al admin de liberar reserva (si hubo), y mandar un "chau me desconecte" al cliente

hacer nuevo tipo de mensaje del admin a los clientes (pasando por el cine) avisando cuando un cliente pide (antes de confirmar) asientos, para avisarle a los clientes en esa sala que hay asientos q se ocuparon

|---> para hacer esto, el admin tiene q tener una tabla donde, al haber un mensaje "elegir sala", se agrega una entrada del id del cine q la mando, y cuando le llega la confirmacion del pago o un timeout, borra esa entrada (posiblemente un multimap)

/**************/



diagrama de secuencia y diagrama de arquitectura (procesos + mecanismos ipc)



*** CUANDO ESTE LO IMPORTANTE HECHO, REVISAR PRINTF Y COSAS PARA QUE SEA "AMIGABLE" LA PRESENTACION ***
